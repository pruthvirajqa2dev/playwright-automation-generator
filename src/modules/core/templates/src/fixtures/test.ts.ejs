/**
 * test.ts — Extended Playwright test fixture.
 *
 * Always import { test, expect } from this file rather than directly from
 * @playwright/test. This ensures that when future modules (network-capture,
 * session-guard, etc.) augment the fixture, all test files receive the
 * enhancements automatically — with no import changes required.
 *
 * Current state: transparent re-export (core module only).
 * Future modules will use base.extend() here to add capabilities.
 *
 * Generated by pw-gen v<%- generator.version %>
 */
export { test, expect } from "@playwright/test";
export type { Page, Locator, TestInfo, BrowserContext } from "@playwright/test";
