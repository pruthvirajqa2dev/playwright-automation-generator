// @ts-check
import tseslint from "typescript-eslint";

/**
 * ESLint flat configuration for <%- project.applicationName %> automation.
 *
 * Key rule: @typescript-eslint/no-floating-promises
 * ─────────────────────────────────────────────────
 * A missing `await` before a Playwright call (click, fill, expect, etc.) produces
 * a test that appears to pass while silently skipping the action or assertion.
 * This rule catches the mistake at author time with zero runtime cost.
 *
 * Run:   npm run lint
 * Fix:   npm run lint -- --fix
 *
 * Generated by pw-gen v<%- generator.version %>
 */
export default tseslint.config({
    files: ["src/**/*.ts"],
    extends: [...tseslint.configs.recommended],
    languageOptions: {
        parserOptions: {
            // Required for type-aware rules such as no-floating-promises
            project: "./tsconfig.json",
        },
    },
    rules: {
        // Error on any Promise that is not awaited or explicitly handled.
        // This is the single most impactful lint rule for Playwright test code.
        "@typescript-eslint/no-floating-promises": "error",
    },
});
