import { Locator, TestInfo } from "@playwright/test";
import { BaseComponent } from "./BaseComponent";

/**
 * <%- name %>Component — reusable UI Component for the <%- name %> widget.
 *
 * ─── ADAPT THIS FILE ──────────────────────────────────────────────────────────
 * 1. Replace placeholder locators with your application's actual selectors.
 * 2. Implement business-oriented interaction methods (e.g. search(), select()).
 * 3. Update the `root` selector passed by the consuming Page Object.
 * 4. Delete any methods that do not apply to this component.
 * ─────────────────────────────────────────────────────────────────────────────
 *
 * Component Object conventions:
 *   • Components are bounded to a scoped root Locator — never to a full Page
 *   • Child locators are private readonly fields created via this.find()
 *   • Methods are business-oriented: submit() not clickSubmitButton()
 *   • Use test.step() in test files or Page Objects, not inside Components
 *   • Components carry no domain state and no navigation logic
 *
 * Composition example in a Page Object:
 *
 *   import { <%- name %>Component } from "../components/<%- name %>Component";
 *
 *   class SomePage extends BasePage {
 *     readonly <%- camelName %> = new <%- name %>Component(
 *       this.page.locator('[data-testid="<%- slug %>"]'),  // TODO: update selector
 *       this.testInfo,
 *     );
 *   }
 *
 * Generated by pw-gen v<%- generator.version %>
 */
export class <%- name %>Component extends BaseComponent {

    // ── Locators ──────────────────────────────────────────────────────────────
    // TODO: Replace these placeholders with selectors scoped to the <%- name %> region.
    //       Use this.find(selector) — it scopes relative to the component root.

    private readonly heading = this.find('[data-testid="<%- slug %>-heading"]');
    private readonly primaryAction = this.find('[data-testid="<%- slug %>-primary-action"]');

    constructor(root: Locator, testInfo: TestInfo) {
        super(root, testInfo);
    }

    // ── Assertions ────────────────────────────────────────────────────────────

    /**
     * Assert that the <%- name %> component is visible.
     * TODO: Replace or extend with component-specific visibility assertions.
     */
    async expectVisible(): Promise<void> {
        await this.expectComponentVisible();
    }

    /**
     * Assert that the <%- name %> component is hidden or detached.
     * TODO: Replace or extend with component-specific hidden assertions.
     */
    async expectHidden(): Promise<void> {
        await this.expectComponentHidden();
    }

    // ── Business Methods ──────────────────────────────────────────────────────
    // TODO: Add business-oriented interaction methods for the <%- name %> component.
    //
    // Examples:
    //   async submit(): Promise<void> {
    //     await this.click(this.primaryAction, "<%- name %> submit");
    //   }
    //
    //   async expectHeadingText(expected: string): Promise<void> {
    //     await this.expectText(this.heading, expected);
    //   }
}
