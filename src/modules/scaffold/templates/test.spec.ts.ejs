import { test, expect } from "../fixtures/test";
import logger from "../logging/logger";
// TODO: Import your Page Object once created
// import { <%- name %>Page } from "../pages/<%- name %>Page";

/**
 * <%- slug %>.spec.ts — <%- name %> test suite.
 *
 * ─── ADAPT THIS FILE ──────────────────────────────────────────────────────────
 * 1. Uncomment the <%- name %>Page import above and scaffold the page:
 *      pw-gen add page <%- name %>
 * 2. Instantiate the Page Object inside each test (see commented example below).
 * 3. Replace placeholder steps with business-logic interactions.
 * 4. Add test annotations: test.info().annotations.push({ type: "Suite", ... })
 * 5. Remove TODO markers when steps are fully implemented.
 * ─────────────────────────────────────────────────────────────────────────────
 *
 * Conventions demonstrated:
 *   • Import test and expect from fixtures/test — never from @playwright/test directly
 *   • test.describe() groups related scenarios
 *   • test.step() produces narrative output in the HTML report
 *   • Structured logger calls at each step boundary
 *   • Placeholder assertions with expect() — replace with real assertions
 *
 * Generated by pw-gen v<%- generator.version %>
 */
test.describe("<%- name %>", () => {

    test("TODO: describe the expected behaviour @smoke", async ({ page }, testInfo) => {

        // TODO: Instantiate your Page Object
        // const <%- camelName %>Page = new <%- name %>Page(page, testInfo);

        test.info().annotations.push(
            { type: "Suite",        description: "<%- name %>" },
            { type: "Generated by", description: "pw-gen v<%- generator.version %>" },
        );

        // ── Step 1: Navigate ──────────────────────────────────────────────────
        await test.step("Navigate to <%- name %>", async () => {
            // TODO: Replace with your Page Object: await <%- camelName %>Page.open()
            await page.goto("/");
            logger.info("Navigated to <%- name %>");
        });

        // ── Step 2: Verify page loaded ────────────────────────────────────────
        await test.step("Verify <%- name %> is loaded", async () => {
            // TODO: Replace with a meaningful assertion using your Page Object
            // await <%- camelName %>Page.expectOnPage();
            await expect(page).toHaveURL(/./);
            logger.info("<%- name %> loaded");
        });

        // ── Step 3: TODO — business logic ─────────────────────────────────────
        await test.step("TODO: Add <%- name %> business steps", async () => {
            // TODO: Implement your application-specific steps here
            // Examples:
            //   await <%- camelName %>Page.search("example query");
            //   await <%- camelName %>Page.expectResultCount(5);
            //   await expect(page.getByText("expected result")).toBeVisible();
        });
    });

});
