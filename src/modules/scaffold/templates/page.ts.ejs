import { Locator, Page, TestInfo } from "@playwright/test";
import { BasePage } from "./BasePage";

/**
 * <%- name %>Page — Page Object for the <%- name %> module.
 *
 * ─── ADAPT THIS FILE ──────────────────────────────────────────────────────────
 * 1. Replace placeholder locators with your application's actual selectors.
 * 2. Implement business-oriented methods: submit(), search(), expectSuccess().
 * 3. Update open() with the correct URL path for this page.
 * 4. Delete any methods that are not applicable to this page.
 * ─────────────────────────────────────────────────────────────────────────────
 *
 * Page Object conventions:
 *   • Locators are private readonly class fields — never expose raw locators
 *   • Methods are business-oriented: search() not clickSearchButton()
 *   • Use test.step() in test files, not inside Page Object methods
 *   • Inject Page and TestInfo via the constructor (see BasePage for rationale)
 *
 * Generated by pw-gen v<%- generator.version %>
 */
export class <%- name %>Page extends BasePage {

    // ── Locators ──────────────────────────────────────────────────────────────
    // TODO: Replace these placeholders with selectors for the <%- name %> page.

    private readonly pageHeading: Locator;

    constructor(page: Page, testInfo: TestInfo) {
        super(page, testInfo);

        // TODO: Update selectors to match your application's <%- name %> page
        this.pageHeading = page.getByRole("heading", { level: 1 });
    }

    // ── Navigation ────────────────────────────────────────────────────────────

    /**
     * Navigate to the <%- name %> page.
     * TODO: Update the URL path to match your application.
     */
    async open(): Promise<void> {
        await this.navigateTo("/<%- slug %>");
        await this.waitForPageLoad();
    }

    // ── Assertions ────────────────────────────────────────────────────────────

    /**
     * Assert the <%- name %> page has loaded.
     * TODO: Replace with an assertion specific to this page.
     */
    async expectOnPage(): Promise<void> {
        await this.expectVisible(this.pageHeading, "<%- name %> page heading");
    }

    // ── Business Methods ──────────────────────────────────────────────────────
    // TODO: Add business-oriented methods for the <%- name %> module.
    //
    // Examples:
    //
    //   async search(query: string): Promise<void> {
    //       await this.fill(this.searchInput, query, "search input");
    //       await this.click(this.searchButton, "search button");
    //   }
    //
    //   async expectResultCount(count: number): Promise<void> {
    //       await expect(this.resultRows).toHaveCount(count);
    //   }
    //
    //   async submit(): Promise<void> {
    //       await this.click(this.submitButton, "submit button");
    //   }
}
